#Initialize
git init
git remote add origin git@github.com:whoknowsjas/semsurf.git

#Commit (check-in)
touch README
git add README
git commit -m '?????'
git push -u origin master

#Get (check-out)
git pull origin master

#Misc
git rm --> deletes from 

#Force
git reset --hard HEAD
git clean -f
git pull


